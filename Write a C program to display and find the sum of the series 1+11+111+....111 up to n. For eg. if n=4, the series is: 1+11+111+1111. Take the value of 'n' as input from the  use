#include <stdio.h>
#include <math.h>
int main() {
    int n,i,m=1,sum=0;
    printf("enter number of times to be displayed");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        sum+=m;
        m=(m*10)+1;
        
    }
    
    
    printf("the sequence: %d",sum);
    return 0;

}
